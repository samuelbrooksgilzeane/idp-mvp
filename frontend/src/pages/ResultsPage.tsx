/** Cross-document summary and export. Populated by the summary and export increment. */
export function ResultsPage() {
  return (
    <section className="page-state">
      <strong>Results</strong>
      <span>
        The cross-document summary and Excel export arrive with the batch and export increment.
      </span>
    </section>
  );
}
